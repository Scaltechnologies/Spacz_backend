{
  "name": "spacz-legacy-migration",
  "version": "1.0.0",
  "private": true,
  "description": "One-time migration of the legacy MySQL spacz database into the SPACZ microservices",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "migrate": "node migrate.mjs",
    "dry-run": "node migrate.mjs --dry-run"
  },
  "dependencies": {
    "mysql2": "^3.11.0"
  }
}
