package com.spacz.studyhall.entity;

public enum EnrollmentSource {
    /** Created automatically when a student's online booking is confirmed. */
    BOOKING,
    /** Added by the vendor (walk-in or offline student). */
    WALK_IN
}
